{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Alterar senha de usuário {{usuario.nome}}</h3>
<hr>
<form action="/usuario/trocasenha" method="POST">
    <input type="hidden" name="id" value="{{usuario._id}}">
    <input type="hidden" name="nome" value="{{usuario.nome}}">
    <label for="senha">Senha: </label>
        <input type="password" name="senha" class="form-control" required>
    <label for="senha2">Repita a sua senha: </label>
        <input type="password" name="senha2" class="form-control" required>
    <button type="submit" class="btn btn-success mt-4">Trocar senha</button>        
</form>
